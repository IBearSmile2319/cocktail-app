import {types} from '../types';

const initialState={
    data:[],
    cart:[],
    total:0
}

export default (state=initialState,action)=>{
    switch(action.type){   
        default:
            return state
    }
}